{
  "entities": {
    "AppStateDoc": {
      "title": "AppStateDoc",
      "description": "Enterprise-wide state key configuration document",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "description": "Stored structured key-value state data (may be arrays, maps, strings)"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when this key-value state was last updated"
        }
      },
      "required": [
        "data"
      ]
    }
  },
  "firestore": {
    "/app_state/{key}": {
      "schema": "AppStateDoc",
      "description": "Collection storing all global state configurations for the enterprise admin portal"
    }
  }
}
